<?php

class StorageService {

  public function store($data, $user) {
    /*
    // if user is not valid

    $redis = Redis::connection();

    //$redis->set('data', 'Alain');
    //$redis->expire('data', 15);

    $data_found = false;
    if( ($data = $redis->get('data')) ) {
      $data_found = true;
    }
    */
    return "Hello World!";
  }
}